services:
  node:
    build:
      context: .
      dockerfile: node-agent.Dockerfile
    env_file:
      - .env
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./data:/var/lib/anneal
